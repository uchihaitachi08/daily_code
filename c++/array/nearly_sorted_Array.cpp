#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;
int main(){
	int n;
	cin>>n;
	int *arr = new int[n];
	for(int i=0;i<n;i++){
		cin>>n;
	}
	vector<int>v;
	for(int i=0;i<n;){
		v.push_back(arr[i]);
		if(v.size() != k+1){
			i++;
			continue;
		}
		else{
			
		}
	}
}