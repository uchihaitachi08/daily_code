#include<iostream>
using namespace std;
struct node{
	int data;
	struct node* next;
};
int main(){
	struct node* head = NULL;
	struct node* tail = NULL;
	if(head || !tail)
		cout<<"motherfucker"<<endl;
	return 0;
}